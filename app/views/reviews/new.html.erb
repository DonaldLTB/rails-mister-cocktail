<div class="banner">
  <h1>Add Review</h1>
</div>
<div class="cards-container-new">
  <%= simple_form_for [@cocktail, @review] do |f| %>

    <div class="form-inputs">
      <%= f.input :content %>
      <%= f.button :submit %>
    </div>
  <% end %>
  <a>
  <%= link_to 'Cancel', @cocktail %>
  </a>
</div>
