<h1>Add a Review</h1>

<%= simple_form_for [@cocktail, @review] do |f| %>

  <div class="form-inputs">
    <%= f.input :content %>
    <%= f.button :submit %>
  </div>
<% end %>
<a>
<%= link_to 'Cancel', @cocktail %>
</a>
